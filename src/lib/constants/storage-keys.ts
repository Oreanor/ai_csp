/** localStorage key for UI locale (next-intl). */
export const UI_LOCALE_STORAGE_KEY = "ai-candidate-ui-locale";

/**
 * localStorage key for app theme (`light` | `dark` | `system`).
 * Value is the short key `theme` for compatibility with earlier PoC builds.
 */
export const UI_THEME_STORAGE_KEY = "theme";

/** Auto-play browser TTS after each candidate reply (`"1"` / `"0"`). Key bump resets legacy default to on. */
export const INTERVIEW_AUTO_TTS_STORAGE_KEY = "ai-csp-interview-auto-read-aloud";
